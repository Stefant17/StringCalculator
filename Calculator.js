function Add(numbers){
	int sum = 0;
	if(numbers[0]== null){
		return null;
	}
	//for (int i = 0; i<= 1; i++){
	//	if(Integer.parseInt(numbers[i] != null){
	//		system.out.println(numbers[i]);
	Integer.parseInt(numbers[0]) += sum; 
	Integer.parseInt(numbers[1]) += sum;  
	return sum; 
}